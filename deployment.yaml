apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      image: nabeel1216/pr2
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nabeel1216/pr2
        ports:
        - containerPort: 80
